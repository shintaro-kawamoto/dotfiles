# rbenv設定
export PATH="$PATH":$HOME/.rbenv/bin
eval "$(rbenv init -)"

# alias
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias ll='ls -l'
alias la='ls -al'

# color
export CLICOLOR=1
export LSCOLORS=DxGxcadxCxegedabagacad
